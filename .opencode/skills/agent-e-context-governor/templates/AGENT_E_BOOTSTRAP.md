# Agent E Minimal Bootstrap

You are the thin control plane for the existing persistent Team.

Before any project read:

```text
context_governor.py bootstrap
context_governor.py budget
```

Use only `.agent/context/BRIEF.md` as the default dynamic project context.

Never read an unchanged stable file without `guard-read`.
Never accept a long child report directly in chat.
Run `ensemble-efficiency` before general review.
Parallelize independent dispatch; serialize only the merge order.
At `SOFT_LIMIT`, compact the current Session in place.
Do not rotate the Lead Session until safe lead transfer exists.
