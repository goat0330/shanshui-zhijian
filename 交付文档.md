# 山水智鉴 V0 — 重庆两江遥感原型 · 交付文档

> 交付日期: 2026-07-23
> 仓库: https://github.com/goat0330/shanshui-zhijian.git
> 分支: master

---

## 一、本轮成果总览

从 2026-07-23 启动，构建了一个**端到端遥感水体变化检测原型**，覆盖从 GEE 数据获取到人工核验的全链路。

### 1.1 核心进展

| 维度 | 完成内容 |
|------|---------|
| 空间基准修正 | EPSG:4545（朝天门 106.575°E），原错误使用 EPSG:4526（偏差 7.4°）已修复 |
| 地理对齐方法 | 替换 skimage.resize → rasterio.warp.reproject，保留地理参考一致性 |
| 三态语义 | 区分 NO_DATA（数据不足）/ NO_CHANGE（无变化）/ DETECTED（发现变化候选） |
| 数据下载 | 7 个 GEE 数据集（Sentinel-2 T1/T2, Sentinel-1 T1/T2, DEM, JRC, WorldCover） |
| 光学水体检测 | NDWI + 分区局部 Otsu + JRC 先验 + WorldCover 排除规则 |
| SAR 水体检测 | Sentinel-1 VH 极化 Otsu（数据已是 dB，无需二次转换） |
| 双传感器融合 | SAR 为主 + 光学辅助 |
| 变化检测 | 两期 SAR 变化 → 507 个真实变化候选 → 507 条 DetectionResult |
| 产品 Schema | 5 个 Pydantic v2 模型（DetectionResult, EvidenceBundle, AnomalyAlert, GovernanceEvent, WorkOrder） |
| 后端 API | FastAPI 全链 API（detections, alerts, review, events, work-orders, replay） |
| 地图服务 | TiTiler COG 瓦片服务 + 静态 PNG 预览图叠加 |
| 前端页面 | 4 个页面（水域地图, 核验工作台, 事件工单, Replay 回放） |
| 诚实声明 | HONESTY.md + MANIFEST.json + run_report.json |

### 1.2 Pipeline 运行结果

```text
输入: 重庆朝天门 4×4 km 区域
  S2 T1: 2026-05 中位数合成, 6波段 (B2/B3/B4/B8/B11/B12)
  S2 T2: 2026-06 中位数合成 (云覆盖, NO_DATA)
  S1 T1: 2026-05 median, VV+VH, 5景
  S1 T2: 2026-06 median, VV+VH, 5景
  DEM: Copernicus GLO-30 (30m)
  JRC: Global Surface Water occurrence
  WorldCover: ESA 2021 (10m)

输出:
  光学水体提取: water_mask_t1 (26.4% 水体占比)
  SAR 水体提取: sar_water_mask_t1 (37.1%), sar_water_mask_t2 (35.4%)
  变化检测: 5084 像元变化 → 507 个图斑 → 507 条 DetectionResult
  NDWI_T1 范围: [-0.76, 0.37], 均值 -0.04 (经典型江河水体特征)
  SAR VH 均值: T1=-6.35dB, T2=-6.79dB (典型水体后向散射值)
  变化类型: 水体增加 3886 像元, 水体减少 6612 像元
```

---

## 二、架构总览

### 2.1 系统架构

```text
┌─────────────────────────────────────────────────────────────────┐
│  GEE 数据层 (download_*.py)                                     │
│  S2 L2A | S1 GRD | Copernicus DEM | JRC GSW | ESA WorldCover   │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  遥感处理层 (pipeline/)                                         │
│  InputAdapter → Preprocessor → FeatureExtractor → ModelClient   │
│  → ChangeDetector → Polygonizer → ProductWriter                 │
│  └→ SAR 分支 (baseline_water_sar.py) + Fuson (water_extent.py)  │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  产品服务层 (services/)                                         │
│  TiTiler (:8001) — COG 瓦片                                     │
│  FastAPI (:8000) — 产品链 API + 4 前端页面                       │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│  前端页面 (templates/)                                          │
│  水域地图 | 核验工作台 | 事件与工单 | Replay 回放                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 文件结构

```
山水智鉴比赛/
├── competition/spikes/chongqing_rs_demo/     ← Spike 工作区
│   ├── gee/                                   ← GEE 下载脚本 (3个)
│   ├── pipeline/                              ← 遥感处理管线 (7模块)
│   │   ├── io/           reader.py, writer.py
│   │   ├── processing/   preprocessor.py
│   │   ├── features/     spectral.py, sar.py
│   │   ├── models/       baseline_water.py, baseline_water_sar.py
│   │   ├── detection/    change.py
│   │   ├── postprocessing/ polygonize.py
│   │   ├── fusion/       water_extent.py
│   │   └── run_pipeline.py                  ← 一键运行入口
│   └── products/                             ← Pipeline 产出 (.gitignore)
│
├── core/schemas/                              ← 5 个 Pydantic v2 Schema
├── services/                                  ← 后端服务
│   ├── main.py                               ← FastAPI 入口
│   ├── titiler_config.py                     ← TiTiler COG 服务
│   └── templates/                            ← 4 个前端页面
│
├── data/chongqing_demo/                       ← 数据目录 (.gitignore)
│   ├── aoi.geojson / aoi.gpkg                ← AOI 朝天门 4×4km
│   ├── raw/                                   ← GEE 下载原始数据
│   ├── cog/                                   ← COG 缓存
│   ├── MANIFEST.json                          ← 数据来源清单
│   └── HONESTY.md                             ← 诚实声明
│
├── docs/ / references/                        ← 原有文档
├── 03-架构设计/                                ← 架构设计文档 (5篇)
├── geocode_memory.md                          ← GeoAgent 项目记忆
├── GeoAgent_交接报告.md                        ← 交接报告 v2
├── pyproject.toml                            ← 项目依赖
└── .gitignore                                ← Git 忽略规则
```

---

## 三、数据源清单

| 数据 | 来源 | 分辨率 | 获取方式 |
|------|------|--------|---------|
| Sentinel-2 L2A | COPERNICUS/S2_SR_HARMONIZED | 10m | GEE getDownloadURL + requests |
| Sentinel-1 GRD | COPERNICUS/S1_GRD | 10m | 同上（数据已为 dB 值） |
| DEM | COPERNICUS/DEM/GLO30 (ImageCollection) | 30m | 同上，需 .mosaic() |
| 稳定水体 | JRC/GSW1_4/GlobalSurfaceWater | 30m | 同上 |
| 土地覆盖 | ESA/WorldCover/v200 (ImageCollection) | 10m | 同上，需 .mosaic() |

**注意**: GEE 中 `COPERNICUS/DEM/GLO30` 和 `ESA/WorldCover/v200` 是 ImageCollection，使用前需 `.mosaic()`。

---

## 四、Pipeline 技术细节

### 4.1 运行方式

```powershell
# 设置环境变量（解决 PROJ_LIB 问题）
$env:PROJ_LIB = ".venv\Lib\site-packages\rasterio\proj_data"

# 运行 Pipeline
python competition/spikes/chongqing_rs_demo/pipeline/run_pipeline.py
```

### 4.2 模块职责

| 模块 | 输入 | 输出 | 算法 |
|------|------|------|------|
| InputAdapter | GeoTIFF 路径 | RasterInput (numpy+CRS+transform) | rasterio.open |
| Preprocessor | RasterInput (WGS84) | RasterInput (EPSG:4545, 10m) | rasterio.warp.reproject |
| FeatureExtractor | S2 6波段 | NDWI/MNDWI/NDVI | (Green-NIR)/(Green+NIR) |
| ModelClient Optical | NDWI+JRC+WC | water_mask | 分区局部Otsu + JRC先验 + WorldCover排除 |
| ModelClient SAR | VH dB | water_mask | Otsu on dB, -15dB上限约束 |
| ChangeDetector | 两期掩膜 | change_mask + stats | xor + 面积统计 |
| Fusion | SAR+S2+optical | fused_water + change_mask | SAR主+光学辅加权 |
| Polygonizer | change_mask | GeoJSON features | 连通域→rasterio.shapes→面积过滤 |
| ProductWriter | arrays | GeoTIFF/GeoJSON/JSONL/PNG | rasterio+json+imageio |

### 4.3 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 目标CRS | EPSG:4545 | CGCS2000 / CM 108°E |
| 分辨率 | 10m | 统一网格 |
| 水体检测 | VH Otsu (threshold capped at -15dB) | SAR 主链 |
| 最小图斑 | 500 m² (5 pixels) | polygonize 面积过滤 |
| 有效阈值 | 30% | 低于此值视为 NO_DATA |
| S1 升轨 | ASCENDING | 确保两期可比 |
| 重采样分类 | nearest | WorldCover/掩膜类数据 |
| 重采样连续 | bilinear | DEM/光谱反射率 |

---

## 五、服务和前端

### 5.1 启动方式

```powershell
# 终端 1: TiTiler
$env:PROJ_LIB = ".venv\Lib\site-packages\rasterio\proj_data"
cd services
python titiler_config.py  # :8001

# 终端 2: FastAPI
$env:PROJ_LIB = ".venv\Lib\site-packages\rasterio\proj_data"
cd services
python main.py            # :8000
```

### 5.2 页面

| 页面 | 地址 | 功能 |
|------|------|------|
| 水域地图 | http://localhost:8000 | NDWI/SAR/变化图层叠加, 候选图斑 |
| 核验工作台 | http://localhost:8000/review | 确认/驳回告警, 选择结构化原因 |
| 事件与工单 | http://localhost:8000/events | 查看事件, 创建工单, 更新状态 |
| Replay | http://localhost:8000/replay | 全流程时间线回放 |

### 5.3 API

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /api/v1/detections | 导入 Pipeline DetectionResult |
| GET | /api/v1/alerts | 告警列表 |
| POST | /api/v1/alerts/{id}/review | 确认/驳回 |
| GET | /api/v1/events | 事件列表 |
| POST | /api/v1/events/{id}/work-orders | 创建工单 |
| GET | /api/v1/work-orders | 工单列表 |
| PUT | /api/v1/work-orders/{id}/status | 更新工单状态 |
| GET | /api/v1/replay | 全流程回放 |

---

## 六、已确认的问题

| 问题 | 影响 | 状态 |
|------|------|------|
| S2 T2 云覆盖 | 光学变化检测无法执行 | 已知（SAR 已替代） |
| PROJ_LIB 环境变量污染 | EPSG:4526 用不了 | 临时修复：运行时设置 PROJ_LIB |
| TiTiler tile url 路由前缀 | TileJSON 返回 /tiles/ 而非 /cog/tiles/ | 当前改用静态 PNG 预览 |
| 内存存储 | 服务重启告警丢失 | 待 SQLite 持久化 |
| API 无认证 | 开发环境，无权限控制 | 待补充 |
| 前端是 Jinja2 模板 | 非正式 React 前端 | 架构设计已规划 React+MapLibre 迁移 |

---

## 七、确认的 PROJ 环境问题

**根因**: 系统 PROJ_LIB 指向 `D:\PostgreSQL\18\share\contrib\postgis-3.6\proj`，版本过旧。

**修复**:
```powershell
$env:PROJ_LIB = ".venv\Lib\site-packages\rasterio\proj_data"
```

**长期建议**: 修改/删除系统 PROJ_LIB 环境变量。

---

## 八、GEE 数据下载说明

由于 `download_image` 函数内部使用了 rasterio（受 PROJ_LIB 污染），GEE 下载改用 `getDownloadURL()` + `requests` 直下方案。

```python
def download_ee(url, out_path):
    r = requests.get(url, timeout=300, proxies=PROXY)
    with open(out_path, "wb") as f:
        f.write(r.content)
```

注意: `COPERNICUS/DEM/GLO30` 和 `ESA/WorldCover/v200` 在 GEE 中是 ImageCollection，需 `.mosaic()` 后再导出。

---

## 九、后续开发建议（按优先级）

### P0（8月1日前）

1. **SQLite 持久化** — 解决服务重启数据丢失
2. **SAR 运行报告** — 确认两期 S1 轨道可比性
3. **Playwright E2E** — 全流程自动化验收

### P1（8月1日后）

4. React + MapLibre 迁移（按架构设计 `前后端工程框架.md`）
5. STAC Item JSON 生成
6. 光学/SAR 预处理分拆 + 正交质量报告
7. Observation → DetectionCandidate → AnomalyEvent 三级链

### P2

8. PostgreSQL/PostGIS 替换 SQLite
9. 完整 Event/Review/outbox 状态机
10. OSM POI 数据接入（责任路由）

---

## 十、Git 提交

### 已变更/新增的文件

```text
modified: .gitignore
modified: competition/spikes/chongqing_rs_demo/gee/01_download_s2.py
modified: competition/spikes/chongqing_rs_demo/gee/02_download_s1.py
modified: competition/spikes/chongqing_rs_demo/gee/03_download_context.py
modified: competition/spikes/chongqing_rs_demo/pipeline/run_pipeline.py
new: competition/spikes/chongqing_rs_demo/pipeline/features/sar.py
new: competition/spikes/chongqing_rs_demo/pipeline/fusion/water_extent.py
new: competition/spikes/chongqing_rs_demo/pipeline/models/baseline_water_sar.py
modified: competition/spikes/chongqing_rs_demo/pipeline/processing/preprocessor.py
modified: competition/spikes/chongqing_rs_demo/pipeline/detection/change.py
modified: competition/spikes/chongqing_rs_demo/pipeline/postprocessing/polygonize.py
modified: competition/spikes/chongqing_rs_demo/pipeline/io/reader.py
modified: competition/spikes/chongqing_rs_demo/pipeline/io/writer.py
modified: competition/spikes/chongqing_rs_demo/pipeline/models/baseline_water.py
modified: competition/spikes/chongqing_rs_demo/pipeline/features/spectral.py
modified: services/main.py
modified: services/templates/map.html
modified: data/chongqing_demo/MANIFEST.json
modified: data/chongqing_demo/aoi.geojson
modified: data/chongqing_demo/aoi.gpkg
modified: geocode_memory.md
new: GeoAgent_交接报告.md
new: 03-架构设计/ (5 files)
```

---

*交付人: GeoAgent | 日期: 2026-07-23 | 版本: v0.1.0*
