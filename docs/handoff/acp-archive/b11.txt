[Compressed conversation section]
**RC-03 执行: 真实 Candidate/Evidence/Summary/EventGeoJSON 存储 (m00760-m00823)**

E 直接在 integration 干活: 创建 `candidate_store.py` (读 C 的 CandidateRecord + 36 条种子数据) → 替换 real_service.py get_candidates/get_candidate 不再回退 mock。循环导入问题(real_service→main→real_service) → 创建独立 candidate_store 文件绕过。SQLite 线程问题 → 改用 _get_session() 每次创建新 session。candidate_store 测试: 36/36 读 OK。79 核心测试通过。commit f3f0d54 → push。

接着修复: get_evidence(读 EvidenceBundleRecord)、get_event_geojson(读 GovernedEventRecord)、get_summary(读 Candidate+Event 计数)、get_candidate_geojson(保留mock 无几何数据)。commit f96a5eb → push。

用户要求: spawn/team_reactivate 并行做。D rc03 (Dashboard 真实 API) + B rc03 (Runs 真实化) → B 汇报完成(run_store.py+real_service.py, 48 tests) + D 汇报完成(client.ts+DashboardPage, npm build)。但分支不可合并 (基于旧 baseline)。

用户怀疑合并意图。当前 E 在重写 run_store.py + wire runs 到 real_service。


<dcp-message-id>b11</dcp-message-id>